name: CI/CD API Java 17 - Deploy to VPS

on:
  push:
    branches:
      - 'release/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Checkout
      - uses: actions/checkout@v4

      # Set up Java 17
      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # Build Spring Boot
      - name: Build with Maven
        run: mvn clean package -DskipTests

      # Generate version tags
      - name: Generate version tags
        id: vars
        run: |
          DATE_TAG=$(date +'%Y.%m.%d')
          SHORT_SHA=$(echo "${GITHUB_SHA}" | cut -c1-7)
          echo "DATE_TAG=$DATE_TAG" >> $GITHUB_ENV
          echo "SHORT_SHA=$SHORT_SHA" >> $GITHUB_ENV

      # Login to GHCR
      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GHCR_TOKEN }}

      # (Opcional) Login to Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Build & Push Docker image
      - name: Build & Push Image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/travelandrepeat-be:latest
            ghcr.io/${{ github.repository_owner }}/travelandrepeat-be:${{ env.DATE_TAG }}
            ghcr.io/${{ github.repository_owner }}/travelandrepeat-be:${{ env.SHORT_SHA }}
            ${{ secrets.DOCKERHUB_USERNAME }}/travelandrepeat-be:latest

      # Deploy to VPS
      - name: Deploy API to VPS via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            echo "== Login to GHCR =="
            echo ${{ secrets.GHCR_TOKEN }} | docker login ghcr.io -u ${{ github.repository_owner }} --password-stdin

            echo "== Pulling latest API image =="
            docker pull ghcr.io/${{ github.repository_owner }}/travelandrepeat-be:latest

            echo "== Stopping old container =="
            docker stop tyr-be || true

            echo "== Removing old container =="
            docker rm tyr-be || true

            echo "== Starting new API container =="
            docker run -d \
              --name tyr-be \
              -p 8080:8080 \
              -e SPRING_PROFILES_ACTIVE=prod \
              -e MAIL_AGENT_TO=${{ secrets.MAIL_AGENT_TO }} \
              -e MAIL_QUOTATION_FROM=${{ secrets.MAIL_QUOTATION_FROM }} \
              -e MAIL_HOST=${{ secrets.MAIL_HOST }} \
              -e MAIL_PORT=587 \
              -e MAIL_PASSWORD=${{ secrets.MAIL_PASSWORD }} \
              -e CORS_ALLOWED_ORIGINS=${{ secrets.CORS_ALLOWED_ORIGINS }} \
              -e RECAPTCHA_SECRET=${{ secrets.RECAPTCHA_SECRET }} \
              -e RECAPTCHA_VERIFY_URL=${{ secrets.RECAPTCHA_VERIFY_URL }} \
              ghcr.io/${{ github.repository_owner }}/travelandrepeat-be:latest

            echo "== API Deployment completed successfully =="
